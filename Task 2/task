Tasks : 
1-  أنشئ برنامجًا يأخذ يومًا من أيام الأسبوع كإدخال ويخبرك ما إذا كان يومًا عمليًا أم يوم عطلة واذا لم يكن اي يوم من ايام الأسبوع اطبع invalid day 
   ( إستخدم Switch )

مثال 

console.log(getDayType('monday')); // Output: Weekday
console.log(getDayType('friday')); // Output: Weekend
console.log(getDayType('Invalid')); // Output: Invalid day

2- أنشئ كائنًا يُمثل شخصًا بخصائص الاسم، العمر، والجنس Object 
مثال 

console.log(person.name); // Output: John Doe
console.log(person.age);  // Output: 30
console.log(person.gender); // Output: Male

3-  أنشئ كائنًا متداخلاً يُمثل سلة التسوق مع العناصر وكمياتها
 Nested Object
مثال 
`
console.log(shoppingCart.items.apple); // Output: 5
console.log(shoppingCart.items.banana); // Output: 3


4- أذكر طريقتين من طرق إنشاء الكائن Object 

5- استخدم الميثود  المناسبة لفحص ما إذا كان النص يحتوي على كلمة او جملة محددة.  String  
```
const sentence = 'The quick brown fox jumps over the lazy dog';

مثال : 'fox'  ابحث عنها واذا كانت موجوده هيرجع true  غير كده هيرجع false

6-  لديك مصفوفة من  الارقام او array  بها مجموعة من الأعداد 
مثال 
const numbers = [1, 2, 3, 4, 5];

بإستخدام map ميثود رجع كل عنصر من العنصر السابقة مضروب في 10 
واطبع النتيجة في console.log 

نفس المثال السابق و بإستخدام filter  رجع اي عنصر من عناصر المصفوفة بشرط يكون اكبر من او يساوي 4